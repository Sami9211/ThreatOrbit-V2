# Optional extras for the staged Postgres backend (DASHBOARD_DB_BACKEND=postgres).
# SQLite (the default) needs none of these. Install alongside the base deps:
#   pip install -r requirements.txt -r requirements-postgres.txt
#
# psycopg       - the Postgres driver (binary build, no system libpq needed)
# psycopg-pool  - connection pool (db_backend pools instead of per-call connect)
# sqlglot       - real SQL parser/transpiler for the SQLite→Postgres dialect layer
psycopg[binary]>=3.3.5,<4
psycopg-pool>=3.3.1,<4
sqlglot>=30.17.0,<31
